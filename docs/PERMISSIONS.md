# Permissions status

permissions and role_permissions already exist, but both contained zero records at inspection. AUTH-1 preserves them and does not invent or seed grants. Permission matrix, editable grants and multiple active roles belong to AUTH-2/3. Do not infer backend authorization from navigation or assume all authenticated users may perform mission actions.
